# macOS-specific files.
.DS_Store

# Ignore all dotenv files by default to prevent accidentally committing any secrets.
# To include specific dotenv files, use the `!` operator or adjust these rules.
.env
.env.*

# Don't ignore example dotenv files.
!.env.example
!.env.*.example
recursos/
no-leer/

# Credenciales y notas locales del equipo Alivia — NUNCA commitear
niveles.txt
*.secret
*.key
*.pem
secrets/
credenciales/
bots/telegram/node_modules/
bots/discord/node_modules/
bots/*/package-lock.json

